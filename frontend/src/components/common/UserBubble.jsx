/**
 * frontend/src/components/common/UserBubble.jsx
 *
 * Chat bubble for a user message in the conversation.
 *
 * Styling:
 *   background: #2a2a2a (--color-you — dark, distinct from model bubbles)
 *   text: #e8e8e8 (--color-text-primary)
 *   alignment: right-aligned
 *
 * Used in both the intake conversation (IntakeFlow)
 * and the roundtable session (SessionView).
 */

import React from "react";

/**
 * @param {Object} props
 * @param {string} props.content - the user's message text
 */
function UserBubble({ content }) {
  // TODO: render right-aligned user message bubble
  return null;
}

export default UserBubble;
